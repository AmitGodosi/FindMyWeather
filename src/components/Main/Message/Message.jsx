import classes from "./Message.module.scss";

const Message = () => {
  return (
    <div className={classes.container}>
      <p> hover / click to see Night Temperature </p>
    </div>
  );
};

export default Message;
